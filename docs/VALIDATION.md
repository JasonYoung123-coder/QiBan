# 0.3.0 验证记录（2026-09-14）

- 后端 46 项、前端 11 项测试通过，生产构建和 Python 静态检查通过。
- 官方音色表核对 6 个 Seed TTS 2.0 音色 ID，真实设置页逐项选择、保存和回读通过；未使用个人密钥向云端合成 6 份样音。
- Hiyori 与 Natori 两个模型实际渲染，女性/男性形象和人设切换、保存、页面重载保持通过；截图人工检查通过。
- 指引链接通过桌面消息通道打开固定官方地址，任意 file / javascript / 非允许地址被拒绝。测试中拦截浏览器启动，仅核对目的地址。
- 新便携目录外层只有 app 和启动脚本；嵌入 Python 的依赖导入与 VAD 加载通过。测试在不提供开发环境的中文空格目录完成。
- 旧版数据库新增 character_id 的迁移及重复启动通过，保留原人设和记录；新安装默认推荐 DeepSeek，已配置的服务参数保持不变。

---

# 0.2.0 验证记录（2026-09-11）

- 后端 43 项测试通过，前端 8 项测试通过；生产构建及 Python 静态检查通过。
- 三种接口用本地模拟服务验证；覆盖真实设置页的选择、输入密钥、测试、保存及保存后密钥不回显。
- 便携运行使用官方 Python 3.12.10 嵌入包和 Electron 41.0.3。验证时 PATH 仅保留 Windows 系统目录，PYTHONHOME / PYTHONPATH / VIRTUAL_ENV 指向不存在位置，仍可启动应用。
- 第一份副本路径包含中文和空格；Live2D 加载、透明桌宠、置顶、窗口恢复、沙箱与预加载隔离均通过。Windows 中英音频分别生成并被 Electron 解码，时长约 2.59 / 2.72 秒。
- 第二份副本省略 Chromium 用户目录，仍通过便携会话身份恢复原人设、记忆和模型密钥配置，验证没有依赖原电脑 Cookie 加密。
- 新增适配器没有使用用户真实密钥访问 OpenAI / Anthropic / 火山服务；云端可用性由用户填写配置后使用连接测试确认。完整 LiveKit 端到端通话仍需真实服务联调。
- 迁移验证是在当前 Windows 上隔离环境完成，尚未在另一台物理电脑测试。

以下保留早期版本验证记录。

---

# 验证记录

## 2026-09-11：火山引擎音色接入

新增 15 项测试，覆盖用户指定的音色与请求契约、任意 UTF-8 / JSON 网络分片、base64 校验、错误和缺失结束状态、空音频与大小上限、密钥缺失状态、API 提供方选择及错误脱敏、LiveKit 的 MP3 发射适配。使用模拟响应，不发起真实火山引擎请求。原有测试与前端构建也纳入本次回归；没有实际 key，未试听音色。

日期：2026-09-10。测试使用临时 profile / 数据库，测试数据不随运行目录交付。

## 已完成的自动检查

- 前端 TypeScript 校验与 Vite 生产构建通过。
- Ruff 后端静态检查通过。
- Python 11 项测试通过：显示前缀确认、用户隔离、重复请求、记忆修改 / 删除 / 跨会话读取、来源限制、删除记忆取消生成、网关请求契约、错误脱敏、输入校验、语音租约权限与失效、上游流中途结束。
- 前端 8 项测试通过：旧回复与序号隔离、跨 UTF-8 / CRLF 分片 SSE、HTTP 错误、模型目录边界 / 远程引用拦截 / 资源释放。
- LiveKit worker 可导入，已按安装的 Agents 1.8.0 检查主要调用签名。
- Electron 实例实际启动，检查 preload、renderer 无 Node 权限、sandbox、桌宠切换、置顶与窗口尺寸恢复。Windows 显示缩放会有 1–2 像素的尺寸取整。
- Windows 本地中英合成成功：中文 3.02 秒、英文 3.08 秒，均为 22,050 Hz WAV。生成文件可用于试听，但不代表经过真人音质评分。

## UI 与画面

浏览器已实测 Hiyori 真实显示、中文示范流式对话、自定义人设、记忆保存及新会话英文读取、记忆修改和删除、刷新后的人设保留。页面对缺少 STT / LiveKit 服务给出明确说明。

隐藏 Electron 窗口的 compositor 截图可能停留在加载帧，因此不能把隐藏窗口截图当作模型已渲染的证据。显示窗口后的截图另行保存到 `output/qa`；运行时 IPC 检查与视觉检查分别记录。

已从用户指定的最终目录重新运行桌面与中英合成检查，确认复制后的依赖和模型可用。`output/qa/desktop-chat.png`、`desktop-pet.png` 为实际显示窗口截图，已目视检查真实人物与透明背景；`desktop-smoke.json`、`speech-smoke.json` 为运行结果。该目录只含测试截图和固定示例音频，不含用户语音。

## 本次尚不能声称完成

- 聊天接口使用 MockTransport 验证；真实服务的网络、账号权限与响应质量需配置后确认。
- 无外部 STT、TTS、LiveKit 凭据，未完成真实麦克风识别、WebRTC 语音通话与打断的端到端验证。
- 自然度、人设稳定性、共情体验与长期记忆质量需要真实模型和用户试聊；示范模式不用于此类评估。
- 尚未做多租户公网、并发负载、长时挂机、多屏 / DPI 矩阵、安装包签名或自动更新验证。
- 自定义模型导入的路径解析有测试；所有第三方模型兼容性未穷举。

## 已知提示

Vite 提示主包超过 500 KB，主要来自 Pixi 和 LiveKit；本地应用可运行，后续可按需加载实时语音模块。Starlette 测试适配器有 httpx 迁移提醒，当前测试通过。受限测试环境不能正常启动部分 Windows 图形 / 语音组件，相关原生验证在常规本机进程中完成，没有为产品禁用 Electron sandbox。
